Two special operators influences the replacement process. First, if an occurance of a parameter in the replacement token sequence is immediately preceded by '#', string quotes(") are placed around the corresponding parameter and the then both the '#' and the parameter identifier are replaced by the quoted argument. A '\' character is inserted before each '"' or '\' character that apeears surrounding or inside, a string literal or character constant in the argument.
